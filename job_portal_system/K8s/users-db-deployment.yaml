apiVersion: apps/v1
kind: Deployment
metadata:
  name: users-db
  namespace: job-portal
spec:
  replicas: 1
  selector:
    matchLabels:
      app: users-db
  template:
    metadata:
      labels:
        app: users-db
    spec:
      containers:
        - name: users-db
          image: postgres:16-alpine
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: "users_db"
            - name: POSTGRES_USER
              value: "jp_user"
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: job-portal-secrets
                  key: DB_PASSWORD